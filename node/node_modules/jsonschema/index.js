function validate() {
  // oni-save-parser uses this only as a guardrail when writing.
  // For our editor use-case, we trust the parsed object structure.
  return { valid: true, errors: [] };
}

module.exports = { validate };
